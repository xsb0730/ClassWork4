<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".MainActivity">

    <!-- 标题 -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="性能监控SDK"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textColor="@color/design_default_color_primary"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp"/>

    <!-- 副标题 -->
    <TextView
        android:id="@+id/tvSubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="流畅性 + ANR 监控测试"
        android:textSize="14sp"
        android:alpha="0.7"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"/>

    <!-- 分割线 -->
    <View
        android:id="@+id/divider"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="?android:attr/listDivider"
        app:layout_constraintTop_toBottomOf="@id/tvSubtitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"/>

    <!-- 测试按钮组 -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnTestJanky"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="测试卡顿 (UI线程耗时)"
        android:textSize="16sp"
        app:icon="@android:drawable/ic_dialog_alert"
        app:iconGravity="start"
        app:layout_constraintTop_toBottomOf="@id/divider"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnTestANR"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="测试ANR (主线程阻塞5秒)"
        android:textSize="16sp"
        app:icon="@android:drawable/ic_menu_close_clear_cancel"
        app:iconGravity="start"
        app:layout_constraintTop_toBottomOf="@id/btnTestJanky"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnHeavyAnimation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="测试重度动画"
        android:textSize="16sp"
        app:icon="@android:drawable/ic_media_play"
        app:iconGravity="start"
        app:layout_constraintTop_toBottomOf="@id/btnTestANR"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp"/>

    <!-- 控制按钮 -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnStopMonitor"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="停止监控"
        android:textSize="16sp"
        app:backgroundTint="#E53935"
        app:icon="@android:drawable/ic_media_pause"
        app:iconGravity="start"
        app:layout_constraintTop_toBottomOf="@id/btnHeavyAnimation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnStartMonitor"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="启动监控"
        android:textSize="16sp"
        app:backgroundTint="#43A047"
        app:icon="@android:drawable/ic_media_play"
        app:iconGravity="start"
        app:layout_constraintTop_toBottomOf="@id/btnStopMonitor"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp"/>

    <!-- 状态显示 -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:cardCornerRadius="8dp"
        app:cardElevation="2dp"
        app:layout_constraintTop_toBottomOf="@id/btnStartMonitor"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="16dp">

        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="状态: 监控运行中\n\n📊 请在Logcat中查看监控报告\n🔍 过滤标签: PerformanceMonitor"
            android:textSize="14sp"
            android:lineSpacingMultiplier="1.3"
            android:padding="16dp"/>

    </com.google.android.material.card.MaterialCardView>

</androidx.constraintlayout.widget.ConstraintLayout>